repo_id: "amanuelbyte/african_speech_dataset_new_uncleaned"
hf_token: null           # set via: export HF_TOKEN=hf_xxx (recommended)
moz_api_key: null        # set via: export MOZ_API_KEY=... (or set here)

data_dir: "./processed_data"
num_proc: 2
push_max_shard_size: "500MB"

audio:
  sampling_rate: 16000
  min_duration: 0.5
  max_duration: 30.0
  filter_by_duration: true   # duration is computed from headers/bytes (no torchcodec)

text:
  min_chars: 2
  max_chars: 500
  lowercase: true

datasets:
  - name: sib_fleurs_zul
    enabled: true
    source: hf
    language: zul
    hf_path: WueNLP/sib-fleurs
    subset: zul_Latn
    split: "train+validation"
    audio_col: audio
    text_col: transcription

  - name: common_voice_zul
    enabled: true
    source: hf
    language: zul
    hf_path: mozilla-foundation/common_voice_17_0
    subset: zu
    split: "train+validation+test"
    audio_col: audio
    text_col: sentence

  # GATED example (keep disabled unless you have access):
  - name: za_african_next_voices_zul
    enabled: false
    source: hf
    language: zul
    hf_path: dsfsi-anv/za-african-next-voices
    subset: zul
    split: "train"
    audio_col: audio
    text_col: transcription

  - name: badrex_zulu_speech
    enabled: true
    source: hf
    language: zul
    hf_path: badrex/zulu-speech
    subset: default
    split: "dev"
    audio_col: audio
    text_col: transcript

  - name: common_voice_mozilla_24_zul
    enabled: true
    source: mozilla_cv
    language: zul
    moz_dataset_id: "cmj8u3q3b00vxnxxbilr9nfyu"
    cv_folder: "zu"
    audio_col: audio
    text_col: text
